<!-- Views/Home/Index.cshtml o cualquier archivo HTML -->
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>SportTime - Home</title>
    <style>
        body {
            background: linear-gradient(135deg, #00e6e6 0%, #00bfae 100%);
            min-height: 100vh;
            margin: 0;
            font-family: 'Segoe UI', Arial, sans-serif;
        }
        .container {
            max-width: 600px;
            margin: 60px auto;
            background: rgba(255,255,255,0.95);
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.18);
            padding: 40px 30px 32px 30px;
        }
        .title {
            font-size: 2.5rem;
            font-weight: 700;
            color: #009688;
            margin-bottom: 32px;
            letter-spacing: 1px;
            text-align: center;
        }
        .menu-btn {
            display: block;
            width: 100%;
            margin: 18px 0;
            padding: 22px 0;
            font-size: 2rem;
            font-weight: 700;
            color: #fff;
            background: linear-gradient(90deg, #009688 60%, #00796b 100%);
            border: none;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.10);
            cursor: pointer;
            transition: background 0.2s, transform 0.1s;
        }
        .menu-btn:hover {
            background: linear-gradient(90deg, #00796b 60%, #009688 100%);
            transform: translateY(-2px) scale(1.03);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="title">SportTime</div>
        <button class="menu-btn" onclick="location.href='@Url.Action("Index", "Turnos")'">Turnos</button>
        <button class="menu-btn">Canchas</button>
        <button class="menu-btn">Clientes</button>
        <button class="menu-btn">Productos</button>
    </div>
</body>
</html>